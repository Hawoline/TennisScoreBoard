# @name create matches and players
POST http://localhost:8080/matches
Content-Type: application/json

{
  "firstPlayerName": "Belikto",
  "secondPlayerName": "Bayarto"
}
> {%
client.global.set("match_uuid", response.body.id)
 %}
###
GET http://localhost:8080/matches

###
GET http://localhost:8080/matches?page=1&player_name=Nel

###
GET http://localhost:8080/matches/{{match_uuid}}

###
GET http://localhost:8080/matches/3rffrf

###
GET http://localhost:8080/matches/match

### @name win match

< {%
const match = client.global.get("match_uuid")
    request.variables.set("id", [
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match,
        match
    ])
%}
POST http://localhost:8080/matches/{{id}}/point
Content-Type: application/json

{
  "name": "Belikto"
}